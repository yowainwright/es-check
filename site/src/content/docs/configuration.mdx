---
title: Configuration
description: How to configure ES Check using .escheckrc files
---

ES Check can be configured using a `.escheckrc` file in your project root. This allows you to define consistent settings that can be version controlled and shared across your team.

## Configuration File

Create a `.escheckrc` file in your project root:

```json
{
  "ecmaVersion": "es6",
  "module": false,
  "files": "./dist/**/*.js",
  "not": ["./dist/skip/*.js"],
  "allowHashBang": false,
  "checkFeatures": true
}
```

## Configuration Options

All command-line options can be specified in the configuration file:

| Option               | Type           | Description                                                                          |
| -------------------- | -------------- | ------------------------------------------------------------------------------------ |
| `ecmaVersion`        | String         | ECMAScript version (e.g., "es5", "es6", "es2020")                                    |
| `files`              | String/Array   | Files or patterns to check                                                           |
| `module`             | Boolean        | Parse files as ES modules                                                            |
| `not`                | Array          | Files or patterns to exclude                                                         |
| `allowHashBang`      | Boolean        | Allow hash bang in files                                                             |
| `checkFeatures`      | Boolean        | Check ES features and, with `checkBrowser`, support in target browsers               |
| `checkForPolyfills`  | Boolean        | Consider polyfills when checking                                                     |
| `ignorePolyfillable` | Boolean/String | Ignore polyfillable features; set to library name (e.g., `"core-js"`) to limit scope |
| `ignore`             | Array          | Features to ignore                                                                   |
| `ignoreFile`         | String         | Path to JSON file with features to ignore                                            |
| `allowList`          | Array          | Features to allow                                                                    |
| `checkBrowser`       | Boolean        | Select an ES target from Browserslist; use with `checkFeatures` for browser checks   |
| `browserslistQuery`  | String         | Custom browserslist query                                                            |
| `browserslistPath`   | String         | Custom path to browserslist config file                                              |

## Browser Feature Checks

Enable both options to check detected features against individual target browsers as well as the
ES version selected by Browserslist:

```json
{
  "checkBrowser": true,
  "checkFeatures": true,
  "browserslistQuery": "last 2 versions",
  "files": ["dist/**/*.js"]
}
```

See [Browserslist integration](/docs/browserslist#feature-level-browser-checks) for compatibility data rules.

## Multiple Configurations

For projects with different build targets, use an array configuration:

```json
[
  {
    "ecmaVersion": "es5",
    "files": "dist/legacy/**/*.js"
  },
  {
    "ecmaVersion": "es2020",
    "module": true,
    "files": "dist/modern/**/*.js"
  }
]
```

## Custom Config Path

Use a custom configuration file location:

```bash
es-check --config=./config/es-check-production.json
```

## Priority

Command-line arguments override configuration file settings:

```bash
# Uses es6 instead of config file's ecmaVersion
es-check es6 --config=.escheckrc
```
